## Version
0.1.75

## Summary
- Defined `LearningAiEngineProvider` interface and implemented `HttpLearningAiEngineProvider` over OpenCode JSON-RPC HTTP API with conditional bean loading and fallback handling
- Wired `OPENCODE_API_KEY` from OCI Vault (`openrouter-api-key` secret) through deploy pipeline: deploy.sh resolves by OCID/name, injects into runtime env, backend container receives it and passes to OpenCode session payload
- Persisted generation status tracking (PENDING/COMPLETED/FAILED), error reasons, and fallback detection in the database

## Verification
- `mvn test` — 87 tests pass, 0 failures, 0 errors
- `mvn compile` — clean build

## Jira
- `AUTO-464`
