name: Deploy Private Host

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: GHCR tag to deploy for the backend image
        required: true
        default: main

permissions:
  contents: read

env:
  DEPLOY_PATH: /opt/autoforge/private

jobs:
  deploy-private:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare SSH client
        env:
          SSH_KEY: ${{ secrets.OCI_SSH_PRIVATE_KEY }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519

      - name: Upload private stack files
        env:
          PUBLIC_HOST: ${{ secrets.OCI_PUBLIC_HOST }}
          PUBLIC_USER: ${{ secrets.OCI_PUBLIC_USER }}
          PRIVATE_HOST: ${{ secrets.OCI_PRIVATE_HOST }}
          PRIVATE_USER: ${{ secrets.OCI_PRIVATE_USER }}
        run: |
          SSH_COMMON=(-o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i "$HOME/.ssh/id_ed25519")
          JUMP_TARGET="$PUBLIC_USER@$PUBLIC_HOST"
          TARGET="$PRIVATE_USER@$PRIVATE_HOST"
          ssh "${SSH_COMMON[@]}" -J "$JUMP_TARGET" "$TARGET" "mkdir -p '$DEPLOY_PATH'"
          scp "${SSH_COMMON[@]}" -J "$JUMP_TARGET" infra/compose/docker-compose.private.yml "$TARGET:$DEPLOY_PATH/docker-compose.private.yml"
          scp "${SSH_COMMON[@]}" -J "$JUMP_TARGET" infra/deploy/private/deploy.sh "$TARGET:$DEPLOY_PATH/deploy.sh"
          ssh "${SSH_COMMON[@]}" -J "$JUMP_TARGET" "$TARGET" "chmod +x '$DEPLOY_PATH/deploy.sh'"

      - name: Render private runtime environment
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
          PUBLIC_HOST: ${{ secrets.OCI_PUBLIC_HOST }}
          PUBLIC_USER: ${{ secrets.OCI_PUBLIC_USER }}
          PRIVATE_HOST: ${{ secrets.OCI_PRIVATE_HOST }}
          PRIVATE_USER: ${{ secrets.OCI_PRIVATE_USER }}
        run: |
          SSH_COMMON=(-o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i "$HOME/.ssh/id_ed25519")
          JUMP_TARGET="$PUBLIC_USER@$PUBLIC_HOST"
          TARGET="$PRIVATE_USER@$PRIVATE_HOST"
          {
            echo "BACKEND_IMAGE=ghcr.io/${{ github.repository_owner }}/autoforge/backend"
            echo "BACKEND_IMAGE_TAG=$IMAGE_TAG"
            echo "BACKEND_HOST_PORT=8080"
            echo "BACKEND_CONTAINER_PORT=8080"
            echo "SPRING_PROFILES_ACTIVE=prod"
            echo "JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=75.0 -XX:+UseSerialGC"
          } > /tmp/autoforge-private.env
          scp "${SSH_COMMON[@]}" -J "$JUMP_TARGET" /tmp/autoforge-private.env "$TARGET:$DEPLOY_PATH/.env"

      - name: Upload registry credentials
        env:
          PUBLIC_HOST: ${{ secrets.OCI_PUBLIC_HOST }}
          PUBLIC_USER: ${{ secrets.OCI_PUBLIC_USER }}
          PRIVATE_HOST: ${{ secrets.OCI_PRIVATE_HOST }}
          PRIVATE_USER: ${{ secrets.OCI_PRIVATE_USER }}
          GHCR_DEPLOY_USERNAME: ${{ secrets.GHCR_DEPLOY_USERNAME }}
          GHCR_DEPLOY_TOKEN: ${{ secrets.GHCR_DEPLOY_TOKEN }}
        run: |
          SSH_COMMON=(-o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i "$HOME/.ssh/id_ed25519")
          JUMP_TARGET="$PUBLIC_USER@$PUBLIC_HOST"
          TARGET="$PRIVATE_USER@$PRIVATE_HOST"
          {
            echo "GHCR_USERNAME=$GHCR_DEPLOY_USERNAME"
            echo "GHCR_TOKEN=$GHCR_DEPLOY_TOKEN"
          } > /tmp/autoforge-private.deploy.env
          scp "${SSH_COMMON[@]}" -J "$JUMP_TARGET" /tmp/autoforge-private.deploy.env "$TARGET:$DEPLOY_PATH/.deploy.env"

      - name: Deploy private stack
        env:
          PUBLIC_HOST: ${{ secrets.OCI_PUBLIC_HOST }}
          PUBLIC_USER: ${{ secrets.OCI_PUBLIC_USER }}
          PRIVATE_HOST: ${{ secrets.OCI_PRIVATE_HOST }}
          PRIVATE_USER: ${{ secrets.OCI_PRIVATE_USER }}
        run: |
          SSH_COMMON=(-o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i "$HOME/.ssh/id_ed25519")
          JUMP_TARGET="$PUBLIC_USER@$PUBLIC_HOST"
          TARGET="$PRIVATE_USER@$PRIVATE_HOST"
          ssh "${SSH_COMMON[@]}" -J "$JUMP_TARGET" "$TARGET" "APP_DIR='$DEPLOY_PATH' bash '$DEPLOY_PATH/deploy.sh'"
