{
  "name": "@autoforge/provider-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "OpenRouter provider proxy for Autoforge AI runtime secret isolation",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
