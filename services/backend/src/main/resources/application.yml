spring:
  application:
    name: autoforge-backend
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:keycloak-public.pem

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

autoforge:
  mvp:
    db-path: ${AUTOFORGE_DB_PATH:/data/autoforge.sqlite}
    github:
      token: ${AUTOFORGE_GITHUB_TOKEN:}
      owner: ${AUTOFORGE_GITHUB_OWNER:}
      repo: ${AUTOFORGE_GITHUB_REPO:autoforge}
      base-branch: ${AUTOFORGE_BASE_BRANCH:main}
  security:
    cors:
      allowed-origins: ${AUTOFORGE_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:4173,http://localhost:8081,http://127.0.0.1:8081,https://oci.prodet.org}
