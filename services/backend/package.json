{
  "name": "@autoforge/backend",
  "private": true,
  "version": "0.1.0",
  "description": "Backend service for Autoforge. Runtime pending.",
  "scripts": {
    "dev": "echo \"Backend runtime is not selected yet\" && exit 1",
    "build": "echo \"Backend runtime is not selected yet\" && exit 1",
    "test": "echo \"Backend tests are not configured yet\" && exit 1"
  }
}
